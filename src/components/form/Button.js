import styled from "styled-components";

const Button = styled.button`
  background-color: rgb(66, 35, 255);
  color: white;
  border-radius: 2rem;
  font-size: 1rem;
  padding: 0.75rem 2rem;
  border: 0;
  cursor: pointer;
`;

export default Button;
